apiVersion: apps/v1
kind: Deployment
metadata:
  name: blood-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blood-v2
  template:
    metadata:
      labels:
        app: blood-v2
    spec:
      containers:
      - name: blood-v2
        image: sumedha33887/bloodbank:v2
        ports:
        - containerPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: blood-v2-service
spec:
  type: NodePort
  selector:
    app: blood-v2
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30002